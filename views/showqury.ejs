<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
  .query-container {
    max-width: 700px;
    margin: 40px auto;
    padding: 24px;
    background-color: #f9f9f9;
    border-radius: 12px;
    box-shadow: 0 0 12px rgba(0, 0, 0, 0.1);
    font-family: Arial, sans-serif;
  }

  .query-title {
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 16px;
    color: #333;
  }

  .query-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
    font-size: 14px;
    color: #666;
  }

  .tag {
    background-color: #dedede;
    color: #333;
    padding: 4px 12px;
    border-radius: 16px;
    font-size: 12px;
    display: inline-block;
  }

  .btn-row {
    margin-top: 24px;
    display: flex;
    gap: 16px;
  }

  .btn {
    padding: 8px 16px;
    background-color: #eee;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    font-weight: bold;
    color: #333;
    transition: background-color 0.2s;
    text-decoration: none;
    text-align: center;
  }

  .btn:hover {
    background-color: #ccc;
  }

  .info-box {
    margin-top: 24px;
    padding: 12px 16px;
    background-color: #e0f0ff;
    border-left: 4px solid #2196F3;
    color: #333;
    border-radius: 6px;
  }
</style>

</head>
<body>
    
    <div class="query-container">
  <div class="query-title"><%= seeQury.msg %></div>

  <div class="query-meta">
    <span class="tag"><%= seeQury.category %></span>
    <span>Posted by: <strong><%= seeQury.user %></strong></span>
  </div>

  <div class="btn-row">
    <a href="/allqury/<%= seeQury._id %>/reply" class="btn">Reply</a>
    <button class="btn" onclick="navigator.clipboard.writeText(window.location.href)">Share</button>
  </div>

  <div class="info-box">
    No answers yet. Be the first to reply to this query.
  </div>
</div>

</body>
</html>


